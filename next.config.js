// next.config.js - ĐỔI .ts → .js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Giữ nguyên các config khác
};

module.exports = nextConfig; // 👈 ĐỔI từ export default
